# Invoice-Aging-Report
Invoice aging report built with Python — cleans 205 BD invoice records, handles missing values &amp; duplicates, and categorizes outstanding balances into overdue buckets (30/60/90+ days) using Pandas, NumPy &amp; Matplotlib.
